#
# Default values
#

# Comma separated folder or file (absolute) paths, where taxonomy files are to be looked up. Must be readable by the Mica application
#files=/etc/mica2/custom-taxonomies,/data/custom-taxonomy.yml
